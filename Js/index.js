function validarNip (nip){
    let flag = 1;
	let pin = document.getElementById("pin").value;
    console.log(pin);
    if (pin === nip) {
        flag = 0;
    } else {
        flag = 1;
    }
return flag;
}

function oportunidadesNip(flag) {
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        false
    }
}